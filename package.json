{
  "name": "pat-before-i-merge",
  "version": "0.1.0",
  "private": true,
  "description": "Pat Before I Merge — a Chrome side panel for reviewing GitHub PRs with Claude (cloud), local Claude Code, or local Cursor, without leaving the page",
  "workspaces": [
    "extension",
    "bridge"
  ],
  "scripts": {
    "dev:ext": "npm --workspace extension run dev",
    "build:ext": "npm --workspace extension run build",
    "dev:bridge": "npm --workspace bridge run dev",
    "build:bridge": "npm --workspace bridge run build"
  }
}
